import type { StudySummaryProps } from "../../types"
import StudySummaryContent from "./StudySummaryContent"

export default function StudySummary(props: StudySummaryProps) {
  return <StudySummaryContent {...props} />
}
